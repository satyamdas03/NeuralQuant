import type { Metadata } from "next"; import Script from "next/script"; import { Syne, Space_Mono, Instrument_Serif } from "next/font/google"; import "./globals.css"; import AppShell from "@/components/layout/AppShell"; import WalkthroughProvider from "@/components/onboarding/WalkthroughProvider"; import { SessionProvider } from "@/lib/session-tracker"; const syne = Syne({ subsets: ["latin"], variable: "--font-syne", weight: ["400", "600", "700", "800"], }); const spaceMono = Space_Mono({ subsets: ["latin"], variable: "--font-space-mono", weight: ["400", "700"], }); const instrumentSerif = Instrument_Serif({ subsets: ["latin"], variable: "--font-serif", weight: ["400"], style: ["normal", "italic"], }); export const metadata: Metadata = { title: "QuantAlpha AI — Research-Driven Portfolio Intelligence", description: "Institutional-grade equity research powered by multi-agent AI. Deploy specialized quantitative agents to analyze, debate, and forecast market movements.", manifest: "/manifest.json", icons: { icon: "/icons/icon-512.png", apple: "/icons/icon-192.png", }, }; export const viewport = { themeColor: "#00ffb2", width: "device-width", initialScale: 1, maximumScale: 5, }; export default function RootLayout({ children }: { children: React.ReactNode }) { return ( <html lang="en" className="dark" suppressHydrationWarning> <body className={`${syne.variable} ${spaceMono.variable} ${instrumentSerif.variable} font-sans min-h-screen antialiased`}> {process.env.NEXT_PUBLIC_PLAUSIBLE_DOMAIN && ( <Script defer data-domain={process.env.NEXT_PUBLIC_PLAUSIBLE_DOMAIN} src="https://plausible.io/js/script.js" /> )}
      <SessionProvider>
        <WalkthroughProvider>
          <AppShell>{children}</AppShell>
        </WalkthroughProvider>
      </SessionProvider>
    </body> </html> ); }